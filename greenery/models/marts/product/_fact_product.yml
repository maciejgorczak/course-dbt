version: 2

models:
  - name: fact_product
    description: "Fact table related to products"
    columns:
      - name: session_id
        description: "Browsing session ID, recurrent"

      - name: event_type
        description: "Type of event"

      - name: product_id
        description: "Product UUID"

      - name: product_name
        description: "Name of the product"